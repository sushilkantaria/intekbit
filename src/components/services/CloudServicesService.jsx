import cloudServicesData from '../../data/services/cloudServices';
import ServiceTabs from '../ServiceTabs';

const CloudServicesService = () => (
  <ServiceTabs servicesData={cloudServicesData} />
);

export default CloudServicesService;
